import MatrixGraph from '@ts-alg/structures/graph/matrixGraph-impl'


describe('DepthFirstSearch', () => {
  it('', () => {
    const g = new MatrixGraph([])
    console.log(g)
    assert.ok(1)
  })
})
